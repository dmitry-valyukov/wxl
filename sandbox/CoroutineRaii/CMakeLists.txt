# Проба RAII в корутине. Связи с wxl тут нет намеренно: ни библиотеки, ни
# модулей дерева — только стандартные корутины и консольный вывод, чтобы
# отделить поведение компилятора от чего бы то ни было нашего.
add_executable(sandbox.coroutine-raii main.cpp)
