#pragma once

// The header a file describing UI in the declarative syntax includes: the
// controls, the property tags, the enum shortcuts and the framework's styles.
// Whatever every such file needs is added here rather than to each of them.

#include "aliases.h"
#include "generated/Members.h"
#include "generated/Microsoft.UI.Xaml.Controls.h"
#include "generated/styles.h"
